<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kobolapp.com.kobolapp3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeMenu"></activity>
        <activity android:name=".RegisterFragment" />
        <activity android:name=".TimeSleptFragment" />
        <activity android:name=".fragment_timeslept" />
        <activity android:name=".test2" />
        <activity android:name=".test1" />
        <activity android:name=".LoginFragment">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>